{
  "name": "@playkeep/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo e2e uses source packages",
    "test": "pnpm test:e2e",
    "test:e2e": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@playkeep/sdk": "workspace:*",
    "ethers": "^6.15.0",
    "vitest": "^3.2.4"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "typescript": "^5.8.3"
  }
}
